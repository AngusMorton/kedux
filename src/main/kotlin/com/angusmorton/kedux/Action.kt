package com.angusmorton.kedux

/**
 * Marker interface for an Action. An Action should be serializable so that you can record and replay it.
 *
 * Consider using data classes to implement Action
 */
interface Action